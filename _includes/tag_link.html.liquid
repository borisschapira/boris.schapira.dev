{%- assign slug = include.tag | slugify -%}{%- assign tag_pattern = site.jekyll-archives.permalinks.tag | default: "/tag/:name/" -%}<a href="{{ tag_pattern | replace: ':name', slug }}">{{ include.tag }}{%- if include.nb %} ({{ include.nb }}){%- endif -%}</a>
