{%- assign pathname = include.name | prepend: '/assets/styles/' -%}
{%- assign hash = site.data.hashes.styles[include.name] -%}
{%- include assets/asset.html.liquid name=pathname extension='css' hash=hash -%}