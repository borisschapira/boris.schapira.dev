{%- assign pathname = include.name | prepend: '/assets/scripts/' -%}
{%- assign hash = site.data.hashes.scripts[include.name] -%}
{%- include assets/asset.html.liquid name=pathname extension='js' hash=hash -%}