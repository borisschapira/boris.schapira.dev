{% unless jekyll.environment == "development" %}
{% if include.caption %}{% cloudinary {{ include.path }} alt="{{ include.alt | escape }}" caption="{{ include.caption | replace: '"', '&quot;' }}" %}{% else %}{% cloudinary {{ include.path }} alt="{{ include.alt | escape }}" %}{% endif %}{% else %}
<figure>
  <img src="{{ include.path }}" alt="{{ include.alt | escape }}">
{% if include.caption %}
  <figcaption>{{ include.caption }}</figcaption>
{% endif %}
</figure>
{% endunless %}