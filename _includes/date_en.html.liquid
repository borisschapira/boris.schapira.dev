{% assign d = include.postDate | localize: "%-d", include.locale %}
{% assign m = include.postDate | localize: "%B", include.locale %}
 {{ include.postDate | localize: "%A", include.locale }}
{% case m %}
  {% when 'April' or 'May' or 'June' or 'July' %}{{ m }}
  {% when 'September' %}Sept.
  {% else %}{{ include.postDate | localize: "%b", include.locale }}.
  {% endcase %}
{% case d %}
  {% when '1' or '21' or '31' %} {{ d }}st
  {% when '2' or '22' %} {{ d }}nd
  {% when '3' or '23' %} {{ d }}rd
  {% else %} {{ d }}th
  {% endcase %},
{{ include.postDate | localize: "%Y", include.locale }}
